import FlowMapSessionPage from '@/components/pages/FlowMapSessionPage';

export const metadata = {
  title: 'The Flow Map Session — FourFlowOS',
  description:
    'A 90–120 minute facilitated session for teams of 5–20. Each person maps the twelve conditions that shape their best work and leaves with their own diagnosis — a paper Flow Map plus a personal Flow Profile.',
};

export default function Page() {
  return <FlowMapSessionPage />;
}
